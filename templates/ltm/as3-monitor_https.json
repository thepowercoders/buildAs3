				"{{Class:Monitor}}": {
					"class": "Monitor",
					"monitorType": "https",
					"remark": "{{remark}}",
					"clientTLS": { "use": "{{clientTLS}}" },
					"send": "{{send}}",
					"receiveDown": "{{receiveDown}}",
					"receive": "{{receive}}",
					"interval": {{interval}},
					"timeUntilUp": 15,
					"timeout": {{timeout}},
					"targetPort": {{targetPort}}
				}
